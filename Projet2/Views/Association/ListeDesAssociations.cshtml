@model Projet2.ViewModels.ListSearchAssociationViewModel
@*@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers*@
@{
    ViewBag.title = "Les associations présentes sur AssoNow";
    Layout = "_Layout";
}

<div class="d-flex flex-column w-100">
    <div class="d-flex flex-row w-100">
        <div class="d-flex justify-content-center w-25">
            <div class="d-flex flex-column bg-light rounded w-75" style="height:500px;">
                <div class="d-flex justify-content-md-center p-3">
                    <p>Recherche</p>
                </div>
                @using (Html.BeginForm())
                {
                    <div class="d-flex flex-column p-3">
                        @Html.LabelFor(m => Model.SearchName)
                        @Html.TextBoxFor(m => Model.SearchName, new { onchange = "submit();", @Class = "form-control form-control-lg form-control-borderless " } )
                    </div>
                    <div class="d-flex flex-row p-3 justify-content-md-center">
                        <button type="submit" class="btn btn-info">Rechercher</button>
                    </div>
                }
            </div>
        </div>
        <div class="d-flex flex-column w-75">
            @{
                int i = 0;
                while (i < Model.AssociationsList.Count)
                {
                    <div class="d-flex flex-row justify-content-around w-100">
                        @for (int j = 0; j < 3; j++)
                        {
                            @if (i < Model.AssociationsList.Count)
                            {
                                <div class="card align-items-center w25">
                                    <img class="card-img-top" src="@Model.AssociationsList[i].Image">
                                    <div class="card-body">
                                        <h5 class="card-title">@Model.AssociationsList[i].Name</h5>
                                        <p class="card-text">@Model.AssociationsList[i].Description</p>
                                        <a href="~/Association/Profil?id=@Model.AssociationsList[i].Id" class="stretched-link"></a>
                                    </div>
                                </div>
                            }
                            i++;
                        }
                    </div>
                }
            }
        </div>
    </div>
</div>
