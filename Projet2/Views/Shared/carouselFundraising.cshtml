@model List<Projet2.Models.Fundraising>

<div class="text-center">
    <h4>Les collectes de dons du moment</h4>
</div>

<div class="container my-4">
    <div id="carouselFundraising" class="carousel slide" data-bs-ride="carousel">
        <div class="carousel-inner">
            @{
                string isFirst = "active";
                int i = 0;
                if (Model.Count == 0)
                {
                    <div class="carousel-item active">
                        <div class="d-flex flex-row">
                            @for (int j = 0; j < 4; j++)
                            {
                                <div class="card align-items-center">
                                    <div class="card-body">
                                        <p class="card-text">D'autres projets à venir</p>
                                    </div>
                                </div>
                            }
                        </div>
                    </div>
                }
                while (i < Model.Count)
                {
                    <div class="carousel-item @isFirst">
                        @{
                            isFirst = "";
                        }
                        <div class="d-flex flex-row">
                            @for (int j = 0; j < 4; j++)
                            {
                                @if (i < Model.Count)
                                {
                                    <div class="card align-items-center">
                                        <img class="card-img-top" src="@Model[i].Image">
                                        <div class="card-body">
                                            <h4 class="card-title">@Model[i].Name</h4>
                                            <p class="card-text">@Model[i].Description</p>
                                            <a class="btn btn-primary" href="../Association/Profil?id=@Model[i].Id">Plus de détails</a>
                                        </div>
                                    </div>
                                }
                                else
                                {
                                    <div class="card align-items-center">
                                        <div class="card-body">
                                            <p class="card-text">D'autres projets à venir</p>
                                        </div>
                                    </div>
                                }
                                i++;
                            }
                        </div>
                    </div>
                }
            }
        </div>
        <div>
            <button class="carousel-control-prev" type="button" data-bs-target="#carouselFundraising" data-bs-slide="prev">
                <i class="bi bi-caret-left-fill" aria-hidden="true"></i>
            </button>
            <button class="carousel-control-next" type="button" data-bs-target="#carouselFundraising" data-bs-slide="next">
                <i class="bi bi-caret-right-fill" aria-hidden="true"></i>
            </button>
        </div>
    </div>
</div>